Test-count snapshot — updated 2026-05-14 after pre-cascade test cleanup.

==============================================================================
Non-integration suite (default test run)
==============================================================================

Collected total: 77 tests — ALL PASSING (3 more behind the @integration marker)

Per-file breakdown (when each file runs alone):

  tests/test_auth.py            ........        8 passed   (Task 10)
  tests/test_scraper.py         ................ 16 passed (existing 14 + Task 13 added 2)
  tests/test_pipeline_modes.py  ......          6 passed   (Task 12)
  tests/test_retrieval.py       ............    12 passed  (pre-Sprint)
  tests/test_api.py             19 passed       (existing 8 + Task 11 added 11)
  tests/test_agents.py          16 passed       (GoalDistiller + Phase 1 nodes + section classifier)

Aggregate (non-integration):
  PASSED:  77
  FAILED:  0

==============================================================================
Pre-cascade test cleanup (2026-05-14)
==============================================================================

An earlier pre-cascade architecture had separate LangGraph nodes for PDF
extraction, section classification, explanation and ranking. The pipeline was
later redesigned into the Phase 1 + run_deep_reader() cascade, which removed
those nodes. The tests that targeted them were left failing until this cleanup.

Removed (15 tests against an architecture that no longer exists):
  - test_agents.py × 13: TestPdfExtractorNode, TestSectionClassifierNode,
      TestExplainerNode, TestRankerNode, TestRoutingLogic
      (node_pdf_extractor / node_explainer / node_ranker / RankerOutput /
       route_evaluator_decision / route_critique_decision — all removed)
  - test_api.py × 2: TestSettingsEndpoints::test_put_settings_triggers_distiller_on_goal_change,
      TestPipelineEndpoints::test_trigger_returns_task_id
      (patched `trigger_agent_discovery`, renamed to `run_full_pipeline` in the
       pipeline split — behaviour now covered by the Task 11 tests)

Fixed (3 tests of a live component against a stale contract):
  - test_agents.py × 3: TestGoalDistiller — run_goal_distiller now returns a
      DistilledCriteriaOutput (with a lexical_query field), not a bare list.
      Tests updated to the current contract; GoalDistiller is live and is the
      subject of thesis section 4.1.1, so it keeps real coverage.

Net: 92 collected / 74 pass / 18 fail  ->  77 collected / 77 pass / 0 fail.

==============================================================================
Integration suite (`-m integration`)
==============================================================================

Collected: 3 tests in tests/test_celery_integration.py
With Docker available:
  - test_lock_prevents_concurrent_full_runs            PASSED
  - test_different_users_get_independent_locks         PASSED
  - test_chain_routes_through_real_broker              PASSED
  3 passed in ~67s

Without Docker: the redis_broker fixture detects missing Docker and SKIPS
the whole file via pytest.skip(). Marker also prevents collection in the
default test run (collected → deselected by `-m "not integration"`).

==============================================================================
For thesis section 4.3
==============================================================================

Frame the suite as:
  - 77 unit/functional tests across 6 modules + 3 integration tests, all green
  - 30 of the 77 were created during this work; the rest were inherited and,
    where they targeted the pre-cascade architecture, were migrated or removed
    alongside it (this is the "test suite evolved with the architecture" point)

Tests created during this work:
  - test_auth.py             — 8 tests   (register / login / logout)
  - test_api.py extensions   — 11 tests  (settings validation + pipeline chain)
  - test_pipeline_modes.py   — 6 tests   (light/full mode regression)
  - test_scraper.py windowed — 2 tests   (since/until query encoding)
  - test_celery_integration  — 3 tests   (real Redis broker)
  Total new: 30 tests.