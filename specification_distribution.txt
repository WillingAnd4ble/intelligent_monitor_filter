# Specification Deployment Distribution

Move the specification files from this root planning station into the respective agent directories. These documents serve as the strict ground-truth rules for each tool.

## 1. /antigravity_backend/
*Assigned To: Antigravity (Backend, Database, LangGraph Orchestration)*
- `DATABASE_SPECIFICATION.md` -> Core SQLAlchemy structure.
- `API_SPECIFICATION.md` -> FastAPI router paths and Pydantic IOs.
- `AGENT_ARCHITECTURE_SPECIFICATION.md` -> Langgraph Nodes and Pydantic edges.
- `INFRA_DATAFLOW_SPECIFICATION.md` -> Celery queues, .env, Sequence Flow.
- `ARCHITECTURE_QA_EDGE_CASES.md` -> Fallback logic for Modal and retry logic blocks.

## 2. /claude_testing/
*Assigned To: Claude Code (Test Engine, Mocking, and Benchmarking Framework)*
- `UNIT_TESTING_SPECIFICATION.md` -> Contains the FR testing matrices and pytest mock rules.
- `EVALUATION_BENCHMARK_SPECIFICATION.md` -> Ground truth instructions and Streamlit analytics structure.
- *Copy: `AGENT_ARCHITECTURE_SPECIFICATION.md`* -> Claude needs this to know exactly what Pydantic classes to mock for node tests.
- *Copy: `API_SPECIFICATION.md`* -> Claude needs this to build the `TestClient` integrations.

## 3. /web_ui/ (Pre-existing)
*Assigned To: Cursor (React, Next.js, Styling SPA)*
- *Copy: `API_SPECIFICATION.md`* -> Required mapping to hook up the Axios endpoints reliably.
- `AI_SYSTEM_SPECIFICATION.md` -> Contains section 3: Web UI Specification routing maps.
