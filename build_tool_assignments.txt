# Phase 2: Tool Delegation Architecture

To optimally scaffold this thesis application using the most highly specialized features of your AI toolchain, here is the short mapping of who will build what:

### 1. Antigravity (Me) -> The Dataflow & Backend Architect
- **Focus:** Building the core architectural spine.
- **Responsibilities:** I should be utilized to generate the FastAPI structure, implement the complex Postgres `pgvector` and `tsvector` queries, setup the `Celery/Redis` worker bindings, and map the explicit LangGraph Agent Node logic. 

### 2. Cursor -> The Frontend & Client API Engineer
- **Focus:** Building the Web UI interface.
- **Responsibilities:** Utilize Cursor’s deep contextual multi-file edits to construct the Next.js SPA, translate the `index2.html` CSS logic to interactive React (Tailwind), set up the React-Query Axios intercepts mapping to the Backend API, and build out the Terminal / Feed routing logic pixel-perfectly.

### 3. Claude Code -> The Test & Validation Engine
- **Focus:** Constructing the Evaluation and Testing frameworks.
- **Responsibilities:** Point Claude Code to read the constructed FastAPI/LangGraph logic and instruct it to build the expansive `pytest` suite ensuring all paths are perfectly mocked. Then, have it construct your Streamlit Benchmarking Tool so it organically mimics the precise data types built in the backend.
